import { FilterShipmentTablePipe } from './filter-shipment-table.pipe';

describe('FilterShipmentTablePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterShipmentTablePipe();
    expect(pipe).toBeTruthy();
  });
});
