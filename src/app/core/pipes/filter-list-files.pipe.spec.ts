import { FilterListFilesPipe } from './filter-list-files.pipe';

describe('FilterListFilesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterListFilesPipe();
    expect(pipe).toBeTruthy();
  });
});
