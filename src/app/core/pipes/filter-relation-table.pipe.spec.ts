import { FilterRelationTablePipe } from './filter-relation-table.pipe';

describe('FilterRelationTablePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRelationTablePipe();
    expect(pipe).toBeTruthy();
  });
});
